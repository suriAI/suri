#   Tested with Python 3.10.9

# FastAPI and web server dependencies
fastapi
uvicorn[standard]

# Computer vision and image processing
opencv-python
numpy

# Data validation and serialization
pydantic

# ONNX runtime for anti-spoofing models
onnxruntime

# Tracker dependencies
lap
scipy

# Attendance database dependencies
ulid

# Installer dependencies
pyinstaller

# Windows-specific dependencies for PyInstaller builds
# Note: Only needed on Windows. Install conditionally:
#   Windows: pip install pywin32-ctypes
#   macOS/Linux: Not required
pywin32-ctypes; sys_platform == 'win32'

# Multipart dependencies
python-multipart